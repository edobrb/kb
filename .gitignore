node_modules/
dist/
.env
.env.bak-*
data/
data copy/
*.log
.DS_Store
# kb/ is generated by `npm run sync`; only the hand-written folder is tracked
kb/*
!kb/manually-curated/
# local helper that carries a browser session cookie; never commit it
refresh-dev-portal-token.sh
# generated by `npm run mcp:install -- --out`: absolute paths of one machine, not shareable as-is
claude_desktop_config.json
